# L1.5 audit-seal 审计包 Implementation Plan（阶段 1：收集 + AI-BOM + 输出）

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** spec-runner 加 `audit-seal` 子命令：收集每次变更的审计制品（**只存引用不复制**）+ 生成 CycloneDX AI-BOM → 输出 `.out/audit/<commit>.json`。满足 L1.5 出口准则"审计包完备率 100%"的收集能力。

**Architecture:** 新增 `spec_runner/audit_seal.py`（`collect_ai_bom` 生成 CycloneDX BOM + `collect_audit_package` 收集制品引用+元数据）。cli.py 加 `audit-seal` 子命令（读 .out/risk.json 的 level + 收集 + 写 .out/audit/<commit>.json）。原则：只收集不生产，AI-BOM 用 CycloneDX 标准（不自造）。

**Tech Stack:** Python（spec-runner）· CycloneDX BOM 格式 · pytest（TDD）

**范围（阶段 1）**：收集 + AI-BOM + 输出 JSON。保留期限（R2+永久/R1两年/R0六月）+ verify.yml 自动归档 + 入库 = 后续阶段。

---

## File Structure

- **Create:** `tools/spec-runner/spec_runner/audit_seal.py` — collect_ai_bom（CycloneDX）+ collect_audit_package
- **Modify:** `tools/spec-runner/spec_runner/cli.py` — 加 audit-seal 子命令 + cmd_audit_seal
- **Test:** `tools/spec-runner/tests/test_cli.py` — audit-seal 测试
- **Modify:** `.github/workflows/verify.yml` — 加 audit-seal 步骤（收集后 artifact）

---

## Task 1: collect_ai_bom（CycloneDX AI-BOM）

**Files:**
- Create: `tools/spec-runner/spec_runner/audit_seal.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加到 `tools/spec-runner/tests/test_cli.py`：

```python
def test_collect_ai_bom_返回CycloneDX格式():
    from spec_runner.audit_seal import collect_ai_bom
    bom = collect_ai_bom()
    assert bom["bomFormat"] == "CycloneDX"
    assert bom["specVersion"] == "1.5"
    names = [c["name"] for c in bom["components"]]
    assert "sherpa-onnx-whisper-small" in names  # 端侧转写模型
    assert "agent-spec" in names                  # 意图编译器
    assert "spec-runner" in names                 # 执行器
    assert "opa" in names                         # 策略评估
    for c in bom["components"]:
        assert c["type"] in ("application", "data", "library")
        assert "bom-ref" in c
```

- [ ] **Step 2: 跑红**

Run: `.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py::test_collect_ai_bom_返回CycloneDX格式 -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'spec_runner.audit_seal'`

- [ ] **Step 3: 实现**

Create `tools/spec-runner/spec_runner/audit_seal.py`：

```python
"""审计包（audit-seal）：收集每次变更的审计制品 + 生成 CycloneDX AI-BOM。

原则（L1.5 出口准则）：只收集不生产，全部用现成标准。
- 制品只存引用（evidence/risk/shadow 等），不复制内容（轨迹外置，脱敏在采集侧）
- AI-BOM 用 CycloneDX 格式（模型/工具/提示词作为组件），不自造格式
"""
from __future__ import annotations

import json
import subprocess
import time
from pathlib import Path


def collect_ai_bom() -> dict:
    """生成 CycloneDX AI-BOM：kite 的模型/工具作为组件。"""
    return {
        "bomFormat": "CycloneDX",
        "specVersion": "1.5",
        "components": [
            {"type": "data", "name": "sherpa-onnx-whisper-small",
             "bom-ref": "model:whisper-small", "description": "端侧语音转写模型（DEC-CAP-TRANSCRIPTION 选 A）"},
            {"type": "application", "name": "agent-spec",
             "bom-ref": "tool:agent-spec", "description": "意图编译器（规约→契约）"},
            {"type": "application", "name": "spec-runner",
             "bom-ref": "tool:spec-runner", "description": "契约执行器 + 五态门禁 + 风险分级"},
            {"type": "application", "name": "opa",
             "bom-ref": "tool:opa", "description": "Rego 策略评估（risk.rego 风险分级）"},
        ],
    }
```

- [ ] **Step 4: 跑绿 + commit**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py::test_collect_ai_bom_返回CycloneDX格式 -v
git add tools/spec-runner/spec_runner/audit_seal.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): audit_seal.collect_ai_bom 生成 CycloneDX AI-BOM"
```

---

## Task 2: collect_audit_package（制品引用 + 元数据）

**Files:**
- Modify: `tools/spec-runner/spec_runner/audit_seal.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加到 `tools/spec-runner/tests/test_cli.py`：

```python
def test_collect_audit_package_含制品引用与ai_bom():
    from spec_runner.audit_seal import collect_audit_package
    pkg = collect_audit_package(commit="abc1234567", risk_level="R0")
    assert pkg["commit"] == "abc1234567"
    assert pkg["risk_level"] == "R0"
    assert isinstance(pkg["timestamp"], int)
    # 制品引用（外置，只存路径不复制内容）
    arts = pkg["artifacts"]
    assert arts["evidence"] == ".out/evidence.json"
    assert arts["risk"] == ".out/risk.json"
    assert arts["shadow"] == ".out/shadow.jsonl"
    assert "design_lint" in arts and "evals" in arts and "mutation" in arts
    # AI-BOM 嵌入
    assert pkg["ai_bom"]["bomFormat"] == "CycloneDX"
```

- [ ] **Step 2: 跑红**

Run: `.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py::test_collect_audit_package_含制品引用与ai_bom -v`
Expected: FAIL — `AttributeError: module 'spec_runner.audit_seal' has no attribute 'collect_audit_package'`

- [ ] **Step 3: 实现**

追加到 `tools/spec-runner/spec_runner/audit_seal.py`（`collect_ai_bom` 之后）：

```python
def collect_audit_package(commit: str, risk_level: str) -> dict:
    """收集审计包：制品引用（外置）+ AI-BOM + 元数据。

    制品只存引用路径，不复制内容（L1.5：轨迹外置，脱敏在采集侧）。
    """
    return {
        "commit": commit,
        "timestamp": int(time.time()),
        "risk_level": risk_level,
        "artifacts": {
            "evidence": ".out/evidence.json",       # 五态判定 + 覆盖矩阵
            "risk": ".out/risk.json",               # 风险分级 level/deny
            "shadow": ".out/shadow.jsonl",          # 影子记录
            "design_lint": "docs/ixd/DESIGN.md（verify.yml @google/design.md lint）",
            "evals": "evals/",                       # 评测跑分
            "mutation": ".github/workflows/nightly.yml",  # 变异得分
        },
        "ai_bom": collect_ai_bom(),
    }
```

- [ ] **Step 4: 跑绿 + 全量回归 + commit**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py -k "audit" -v
.venv/bin/python -m pytest tools/spec-runner/tests/ -q
git add tools/spec-runner/spec_runner/audit_seal.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): collect_audit_package 收集制品引用 + AI-BOM"
```

---

## Task 3: audit-seal CLI 子命令（写 .out/audit/<commit>.json）

**Files:**
- Modify: `tools/spec-runner/spec_runner/cli.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加到 `tools/spec-runner/tests/test_cli.py`：

```python
def test_audit_seal命令_写出审计包(monkeypatch, tmp_path, capsys):
    from spec_runner import audit_seal
    monkeypatch.setattr(audit_seal, "collect_audit_package", lambda commit, risk_level: {
        "commit": commit, "risk_level": risk_level, "timestamp": 1,
        "artifacts": {}, "ai_bom": {"bomFormat": "CycloneDX"}})
    monkeypatch.setattr(audit_seal.subprocess, "run", lambda *a, **k: type("R", (), {"stdout": "short12345"})())
    monkeypatch.chdir(tmp_path)
    code = cli.main(["audit-seal"])
    assert code == 0
    out = capsys.readouterr().out
    pkg_file = tmp_path / ".out" / "audit" / "short12345.json"
    assert pkg_file.exists()
    pkg = json.loads(pkg_file.read_text())
    assert pkg["commit"] == "short12345"
```

- [ ] **Step 2: 跑红**

Run: `.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py::test_audit_seal命令_写出审计包 -v`
Expected: FAIL — argparse `invalid choice: 'audit-seal'`

- [ ] **Step 3: 实现**

Modify `tools/spec-runner/spec_runner/cli.py`：

(a) subparser 区加（`p_risk` 之后）：

```python
    sub.add_parser("audit-seal", help="收集审计包（制品引用 + CycloneDX AI-BOM）→ .out/audit/<commit>.json")
```

(b) dispatch dict 加 `"audit-seal": cmd_audit_seal,`

(c) `cmd_risk` 之后加：

```python
def cmd_audit_seal(args) -> int:
    from spec_runner.audit_seal import collect_audit_package
    commit = subprocess.run(
        ["git", "rev-parse", "--short=10", "HEAD"],
        capture_output=True, text=True).stdout.strip() or "unknown"
    risk_level = "?"
    risk_file = Path(".out/risk.json")
    if risk_file.exists():
        try:
            risk_level = json.loads(risk_file.read_text(encoding="utf-8")).get("level", "?")
        except Exception:
            pass
    pkg = collect_audit_package(commit, risk_level)
    out_dir = Path(".out/audit")
    out_dir.mkdir(parents=True, exist_ok=True)
    out_file = out_dir / f"{commit}.json"
    out_file.write_text(json.dumps(pkg, ensure_ascii=False, indent=2), encoding="utf-8")
    print(json.dumps({"audit_seal": str(out_file)}, ensure_ascii=False))
    return 0
```

> 注：`subprocess` 已在 cli.py 顶部？查一下；没有则 `import subprocess`。实际 cli.py 当前无 subprocess import，需加。

- [ ] **Step 4: 跑绿 + 全量回归 + commit**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py -k "audit" -v
.venv/bin/python -m pytest tools/spec-runner/tests/ -q
git add tools/spec-runner/spec_runner/cli.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): audit-seal 子命令（读 risk.json + 写 .out/audit/<commit>.json）"
```

---

## Task 4: CI 接线（verify.yml 加 audit-seal 步骤）

**Files:**
- Modify: `.github/workflows/verify.yml`

> 非 TDD（CI 配置），靠 PR 验证。

- [ ] **Step 1: 加 audit-seal 步骤**

在 `verify.yml` 的 risk gate 步骤**之后**、upload-artifact 之前加：

```yaml
      # L1.5 审计包：收集制品引用 + CycloneDX AI-BOM（只收集不生产）
      - name: audit-seal（L1.5 审计包）
        run: python -m spec_runner audit-seal
```

upload-artifact 的 path 已含 .out/（会自动收集 .out/audit/）。

- [ ] **Step 2: yaml 验证 + 走 PR 验证**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -c "import yaml; yaml.safe_load(open('.github/workflows/verify.yml')); print('yaml OK')"
git checkout -b l1.5-audit-seal
git add .github/workflows/verify.yml
git commit -m "ci(verify): L1.5 审计包收集步骤（audit-seal）"
git push "https://x-access-token:$(gh auth token)@github.com/weiping/kite.git" l1.5-audit-seal
gh pr create --title "L1.5 audit-seal: 审计包收集 + CycloneDX AI-BOM" --body "Task1-4" --base main --head l1.5-audit-seal
```

- [ ] **Step 3: 验证 PR（R3 改 workflow → 人审 merge）**

```bash
sleep 15
gh run watch $(gh run list --workflow verify.yml --branch l1.5-audit-seal --limit 1 --json databaseId -q '.[0].databaseId')
```
Expected: verify 绿，audit-seal 步骤输出 `{"audit_seal": ".out/audit/<commit>.json"}`，artifact evidence 含 audit/。PR R3（改 workflow）→ 自审 merge。

---

## Self-Review

**1. Spec coverage：** L1.5 出口准则"审计包完备率 100%"的**收集能力**——collect_ai_bom（Task 1）+ collect_audit_package（Task 2）+ audit-seal CLI（Task 3）+ CI 接线（Task 4）。CycloneDX AI-BOM（不自造，决策点 2）✓。只存引用（index.md "轨迹外置"）✓。保留期限/入库/自动触发 = 后续阶段（决策点 4）。

**2. Placeholder scan：** 无 TBD。每步完整代码。CycloneDX components 硬编码 kite 已知 AI 组件（sherpa/agent-spec/spec-runner/opa，来自 pubspec/pyproject 调研）。

**3. Type consistency：** `collect_ai_bom() -> dict`、`collect_audit_package(commit, risk_level) -> dict`、`cmd_audit_seal(args) -> int` 一致。测试 monkeypatch 签名匹配（`audit_seal.subprocess.run`、`collect_audit_package(commit, risk_level)`）。

**注意：** cli.py 当前无 `import subprocess`，Task 3 实现时确认/补加。audit_seal.py 顶部已 import subprocess（Task 3 测试 monkeypatch audit_seal.subprocess）。
