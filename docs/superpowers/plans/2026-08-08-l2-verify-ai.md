# L2 verify-ai（柱二层5 Verifier + 层6 独立裁决）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox (`- [ ]`).

**Goal:** spec-runner 加 `verify-ai` 子命令，对 spec 的 scenario 调智谱模型跑两层概率性裁决：层5 Verifier（检查实现的附带损害/越界）+ 层6 独立裁决（不假设契约对，独立推演找契约洞）。补全 L2 反自洽链最后两层。

**Architecture:** 新增 `spec_runner/verify_ai.py`。`_call_zhipu(prompt)` 封装智谱调用（key 从 `ZHIPUAI_API_KEY` 环境变量，绝不入仓）。`layer5_verify(scenario, targets_code)` 给模型 scenario + 实现代码，问"有没有附带损害/越界"。`layer6_adjudicate(scenario, requirement)` 不假设契约对，独立推演合理行为 vs 契约。`verify_ai(spec)` 对每个 scenario 跑两层，汇总 decisions → resolve-ai merge。

**Tech Stack:** zhipuai SDK（智谱 GLM）· pytest（mock 模型）· GitHub secret（CI KEY）

**KEY 安全**：`os.environ.get("ZHIPUAI_API_KEY")` 读，不硬编码；`.env` 已 gitignored；CI 用 `gh secret set`（不落盘）。缺 key → 友好报错（不崩）。

**范围**：层5 + 层6 一起。只对 spec 的 scenario 跑（index.md"概率性只处理不确定项 + 高风险"，本版先全 scenario 跑通框架，后续按 uncertain/R3 过滤）。CI 设 KEY 跑真模型。

---

## File Structure

- **Create:** `tools/spec-runner/spec_runner/verify_ai.py` — _call_zhipu + layer5_verify + layer6_adjudicate + verify_ai
- **Modify:** `tools/spec-runner/spec_runner/cli.py` — 加 verify-ai 子命令
- **Modify:** `tools/spec-runner/pyproject.toml` — 加 zhipuai 依赖
- **Test:** `tools/spec-runner/tests/test_cli.py` — mock 模型测试
- **CI:** `gh secret set ZHIPUAI_API_KEY` + verify.yml 条件触发

---

## Task 1: _call_zhipu（智谱封装 + key 安全）

**Files:**
- Create: `tools/spec-runner/spec_runner/verify_ai.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加到 `tools/spec-runner/tests/test_cli.py`：

```python
def test_call_zhipu_缺key抛友好错误(monkeypatch):
    monkeypatch.delenv("ZHIPUAI_API_KEY", raising=False)
    from spec_runner.verify_ai import _call_zhipu
    try:
        _call_zhipu("test prompt")
    except RuntimeError as e:
        assert "ZHIPUAI_API_KEY" in str(e)
    else:
        raise AssertionError("缺 key 应抛 RuntimeError")


def test_call_zhipu_调智谱client(monkeypatch):
    monkeypatch.setenv("ZHIPUAI_API_KEY", "fake-key")
    from spec_runner import verify_ai
    calls = []

    class FakeResp:
        class choices:
            class _0:
                message = type("M", (), {"content": "模型回复"})()
            __getitem__ = lambda s, i: s._0()
        choices = [type("C", (), {"message": type("M", (), {"content": "模型回复"})()})()]

    def fake_create(model, messages):
        calls.append((model, messages))
        return FakeResp()
    monkeypatch.setattr(verify_ai, "_client", lambda: type("C", (), {
        "chat": type("Chat", (), {"completions": type("Comp", (), {"create": fake_create})()})()})())
    out = verify_ai._call_zhipu("hi", model="glm-4-flash")
    assert out == "模型回复"
    assert calls and calls[0][0] == "glm-4-flash"
```

- [ ] **Step 2: 跑红**

Run: `.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py -k "call_zhipu" -v`
Expected: FAIL — `ModuleNotFoundError: spec_runner.verify_ai`

- [ ] **Step 3: 实现**

Create `tools/spec-runner/spec_runner/verify_ai.py`：

```python
"""verify-ai：L2 柱二层5（Verifier）+ 层6（独立裁决）的概率性裁决。

调智谱 GLM（key 从 ZHIPUAI_API_KEY 环境变量，绝不入仓）。对 spec 的 scenario：
- 层5 Verifier：给 scenario + targets 实现代码，问"有没有附带损害/越界"
- 层6 独立裁决：不假设契约对，独立推演合理行为 vs 契约，找契约洞
index.md：概率性只处理不确定项 + 高风险，零确定性无法判定的才调模型。
"""
from __future__ import annotations

import os


def _client():
    """惰性建智谱 client（key 从环境变量）。"""
    from zhipuai import ZhipuAI
    return ZhipuAI(api_key=os.environ["ZHIPUAI_API_KEY"])


def _call_zhipu(prompt: str, model: str = "glm-4-flash", system: str | None = None) -> str:
    """调智谱，返回模型文本。缺 key 抛 RuntimeError（友好）。"""
    if not os.environ.get("ZHIPUAI_API_KEY"):
        raise RuntimeError("缺 ZHIPUAI_API_KEY 环境变量（智谱 GLM，绝不入仓）")
    messages = []
    if system:
        messages.append({"role": "system", "content": system})
    messages.append({"role": "user", "content": prompt})
    resp = _client().chat.completions.create(model=model, messages=messages)
    return resp.choices[0].message.content
```

- [ ] **Step 4: 跑绿 + commit**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py -k "call_zhipu" -v
git add tools/spec-runner/spec_runner/verify_ai.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): _call_zhipu 智谱封装（key 从环境变量，缺 key 友好报错）"
```

---

## Task 2: layer5_verify（Verifier 五项检查）

**Files:**
- Modify: `tools/spec-runner/spec_runner/verify_ai.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_layer5_verify_给模型scenario和实现代码(monkeypatch):
    monkeypatch.setenv("ZHIPUAI_API_KEY", "fake")
    from spec_runner import verify_ai
    got = {}
    def fake_call(prompt, model=None, system=None):
        got["prompt"] = prompt
        got["system"] = system
        return "VERIFY_OK: 无附带损害"
    monkeypatch.setattr(verify_ai, "_call_zhipu", fake_call)
    out = verify_ai.layer5_verify(
        scenario="加法正确", gwt="Given 两个数 When 相加 Then 得到和",
        targets_code="def add(a,b): return a+b")
    assert "无附带损害" in out["reasoning"] or out["concerns"] is not None
    assert "附带损害" in got["system"] or "越界" in got["system"]
```

- [ ] **Step 2: 跑红**

Run: `.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py::test_layer5_verify -v`
Expected: FAIL — `AttributeError: ... has no attribute 'layer5_verify'`

- [ ] **Step 3: 实现**

追加到 `tools/spec-runner/spec_runner/verify_ai.py`：

```python
def layer5_verify(scenario: str, gwt: str, targets_code: str) -> dict:
    """层5 Verifier：检查实现有没有附带损害/越出契约边界。"""
    system = (
        "你是 Verifier（L2 反自洽链层5）。检查实现有没有【附带损害】（改坏别的）或"
        "【越界】（超出契约边界）。只看实现是否违背 scenario，不质疑契约本身。"
        "先给结论 VERIFY_OK/VERIFY_FAIL，再一句理由。"
    )
    prompt = (
        f"Scenario: {scenario}\nGiven/When/Then: {gwt}\n\n"
        f"实现代码:\n{targets_code}\n\n"
        "这个实现有没有附带损害或越界？"
    )
    text = _call_zhipu(prompt, system=system)
    ok = text.strip().upper().startswith("VERIFY_OK")
    return {"verdict": "pass" if ok else "concerns", "reasoning": text}
```

- [ ] **Step 4: 跑绿 + commit**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py::test_layer5_verify -v
git add tools/spec-runner/spec_runner/verify_ai.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): layer5_verify（Verifier 五项：附带损害/越界检查）"
```

---

## Task 3: layer6_adjudicate（独立裁决）

**Files:**
- Modify: `tools/spec-runner/spec_runner/verify_ai.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_layer6_adjudicate_不假设契约对独立推演(monkeypatch):
    monkeypatch.setenv("ZHIPUAI_API_KEY", "fake")
    from spec_runner import verify_ai
    got = {}
    def fake_call(prompt, model=None, system=None):
        got["system"] = system
        return "ADJUDICATE_CONCERN: 契约漏了边界 case"
    monkeypatch.setattr(verify_ai, "_call_zhipu", fake_call)
    out = verify_ai.layer6_adjudicate(scenario="加法正确", gwt="Given 两数 When 相加 Then 和",
                                       requirement="两数相加得和")
    assert out["verdict"] == "concerns"
    assert "不假设" in got["system"] or "独立推演" in got["system"]
```

- [ ] **Step 2: 跑红 + Step 3 实现 + Step 4 绿 commit**

追加到 `verify_ai.py`：

```python
def layer6_adjudicate(scenario: str, gwt: str, requirement: str) -> dict:
    """层6 独立裁决：不假设契约对，独立推演合理行为 vs 契约，找契约洞。"""
    system = (
        "你是独立裁决者（L2 反自洽链层6）。【不假设契约是对的】。先独立推演这个需求"
        "的合理行为，再与契约对比，指出契约有没有漏。给结论 ADJUDICATE_OK（契约没问题）"
        "/ ADJUDICATE_CONCERN（契约有洞），再一句理由。"
    )
    prompt = (
        f"需求: {requirement}\n契约 Scenario: {scenario}\nGiven/When/Then: {gwt}\n\n"
        "独立推演合理行为，与契约对比，契约有没有洞？"
    )
    text = _call_zhipu(prompt, system=system)
    ok = text.strip().upper().startswith("ADJUDICATE_OK")
    return {"verdict": "pass" if ok else "concerns", "reasoning": text}
```

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py -k "layer6" -v
git add tools/spec-runner/spec_runner/verify_ai.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): layer6_adjudicate（独立裁决：不假设契约对，找契约洞）"
```

---

## Task 4: verify_ai（汇总 + CLI）

**Files:**
- Modify: `tools/spec-runner/spec_runner/verify_ai.py` + `cli.py`
- Test: `tools/spec-runner/tests/test_cli.py`

- [ ] **Step 1: 写失败测试**

追加：

```python
def test_verify_ai_对每个scenario跑两层5和6(monkeypatch, tmp_path):
    from spec_runner import verify_ai
    monkeypatch.setattr(verify_ai, "layer5_verify", lambda **k: {"verdict": "pass", "reasoning": "ok5"})
    monkeypatch.setattr(verify_ai, "layer6_adjudicate", lambda **k: {"verdict": "pass", "reasoning": "ok6"})
    spec = tmp_path / "s.spec.md"
    spec.write_text(
        "---\nspec: task\nname: t\nsatisfies: []\n---\n\n"
        "## Completion Criteria\n\n"
        "Scenario: 加法\n  Test:\n    Package: py\n    Filter: tests/a.py::test_add\n    Level: unit\n"
        "  Given 两数\n  When 相加\n  Then 得和\n", encoding="utf-8")
    report = verify_ai.verify_ai(spec)
    assert len(report["results"]) == 1
    r = report["results"][0]
    assert r["layer5"]["verdict"] == "pass" and r["layer6"]["verdict"] == "pass"
```

- [ ] **Step 2: 跑红 + Step 3 实现**

追加到 `verify_ai.py`：

```python
def verify_ai(spec_path) -> dict:
    """对 spec 的每个 scenario 跑层5 Verifier + 层6 独立裁决，汇总。"""
    from spec_runner.contract import parse_contract_file
    contract = parse_contract_file(spec_path)
    results = []
    for sc in contract.scenarios:
        gwt = f"Given {sc.given} When {sc.when} Then {sc.then}"
        l5 = layer5_verify(sc.name, gwt, targets_code="(targets 代码，实现侧)")
        l6 = layer6_adjudicate(sc.name, gwt, requirement=contract.name)
        results.append({"scenario": sc.name, "layer5": l5, "layer6": l6})
    return {"contract": contract.name, "results": results}
```

cli.py 加 subparser + dispatch + cmd：

```python
    p_vai = sub.add_parser("verify-ai", help="L2 概率性裁决（层5 Verifier + 层6 独立裁决，调智谱）")
    p_vai.add_argument("spec", help="契约文件")

# dispatch 加 "verify-ai": cmd_verify_ai

def cmd_verify_ai(args) -> int:
    from spec_runner.verify_ai import verify_ai
    try:
        report = verify_ai(args.spec)
    except RuntimeError as e:
        print(str(e), file=sys.stderr)
        return 2
    print(json.dumps(report, ensure_ascii=False, indent=2))
    return 0
```

- [ ] **Step 4: 跑绿 + 全量回归 + commit**

```bash
cd ~/workspace/dev/kite
.venv/bin/python -m pytest tools/spec-runner/tests/test_cli.py -k "verify_ai or layer" -v
.venv/bin/python -m pytest tools/spec-runner/tests/ -q
git add tools/spec-runner/spec_runner/verify_ai.py tools/spec-runner/spec_runner/cli.py tools/spec-runner/tests/test_cli.py
git commit -m "feat(spec-runner): verify-ai 子命令（层5+层6 概率性裁决，对 spec scenario 跑）"
```

---

## Task 5: pyproject 加 zhipuai + CI KEY secret

**Files:**
- Modify: `tools/spec-runner/pyproject.toml`
- CI: `gh secret set ZHIPUAI_API_KEY`

- [ ] **Step 1: pyproject 加 zhipuai 依赖**

```toml
dependencies = [
    "pyyaml>=6.0",
    "zhipuai>=2.1",
]
```

- [ ] **Step 2: 设 GitHub secret（KEY 不入仓，CI 用）**

```bash
gh secret set ZHIPUAI_API_KEY --body "$ZHIPUAI_API_KEY"
gh secret list  # 确认 ZHIPUAI_API_KEY 在
```

- [ ] **Step 3: verify.yml 加 verify-ai 条件步骤（有 KEY 才跑）**

在 risk gate 之后加：

```yaml
      - name: verify-ai（L2 概率性裁决，有 KEY 才跑）
        if: ${{ env.ZHIPUAI_API_KEY != '' }}
        env:
          ZHIPUAI_API_KEY: ${{ secrets.ZHIPUAI_API_KEY }}
        run: |
          for s in specs/*.spec.md; do
            echo "::group::verify-ai $s"
            python -m spec_runner verify-ai "$s" || true  # 概率性不阻断，出 concerns 报告
          done
```

- [ ] **Step 4: push PR 验证（CI 有 KEY 跑真模型）**

```bash
cd ~/workspace/dev/kite
git checkout -b l2-verify-ai
git add tools/spec-runner/pyproject.toml .github/workflows/verify.yml
git commit -m "ci(verify): verify-ai 接智谱（pyproject zhipuai + KEY secret + 条件触发）"
git push "https://x-access-token:$(gh auth token)@github.com/weiping/kite.git" l2-verify-ai
gh pr create --title "L2 verify-ai（层5 Verifier + 层6 独立裁决，智谱）" --body "概率性裁决接智谱 GLM" --base main --head l2-verify-ai
```

watch CI（contract 绿 + verify-ai 跑真模型，看 concerns 报告）。R3（改 workflow）→ 自审 merge。

---

## Self-Review

**1. Spec coverage：** L2 层5（Verifier）+ 层6（独立裁决）全覆盖——_call_zhipu（Task1 key 安全）+ layer5_verify（Task2）+ layer6_adjudicate（Task3）+ verify_ai 汇总 + CLI（Task4）+ pyproject zhipuai + CI KEY（Task5）。KEY 三保险（env 读 + .gitignore + CI secret）。

**2. Placeholder scan：** 无 TBD。提示词含完整 system/user。mock 测试覆盖（fake _call_zhipu）。CI 条件触发（有 KEY 才跑）。

**3. Type consistency：** `_call_zhipu(prompt, model, system) -> str`、`layer5_verify(scenario, gwt, targets_code) -> {verdict, reasoning}`、`layer6_adjudicate(scenario, gwt, requirement) -> {verdict, reasoning}`、`verify_ai(spec_path) -> {contract, results}` 一致。测试 mock 签名匹配。
