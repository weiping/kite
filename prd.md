---
title: Kite 产品需求文档（PRD）
version: 0.1.0
status: draft
owner: weiping
last_updated: 2026-08-04
satisfies_charter: charter/MISSION.md
---

# Kite 产品需求文档

> 一句话：**以最低的心智负担捕捉、组织、找回自己的想法。**
> Kite 是一个本地优先的个人 AI 助理——语音或文字速记，自动结构化为笔记/任务/日程，离线可写，多设备同步，自然语言检索与追问。

本 PRD 定义完整功能。后续由 `intent-compile` 技能编译成 `knowledge/requirements/REQ-*.md` 条款，再下降为 `specs/*.spec.md` 任务契约。功能需求采用 EARS 句式（When/While/If/Where + 行为），优先级用 MUST / SHOULD / MAY。

---

## 1. 产品定位

| 维度 | 说明 |
| --- | --- |
| 是什么 | 个人 AI 助理：捕获 → 结构化 → 组织 → 找回 |
| 给谁用 | 想法多、记性差、不想被工具绑架注意力的人（创作者、产品/研发、学生） |
| 核心价值 | 最低心智负担。从"想到"到"记下"零摩擦；从"记过"到"找回"一句话 |
| 平台 | Flutter 双端（iOS / Android），后续桌面 / Web |
| 后端 | Python：Agent 编排层（AI/转录/结构化）+ API Server（同步/调度） |

## 2. 差异化（为什么是 Kite）

市面产品各占一端，没有产品同时满足下面四点：

| 能力 | 语音速记类 | AI 笔记检索类 | 本地优先类 | **Kite** |
| --- | --- | --- | --- | --- |
| 语音→结构化 | ✅ | △ | ✗ | ✅ |
| 自然语言检索/追问 | ✗ | ✅ | ✗ | ✅ |
| 本地优先、离线可写 | ✗ | ✗ | ✅ | ✅ |
| AI 生成可追溯、不冒充确定 | ✗ | ✗ | ✗ | ✅ |

**Kite 的立场**：把"语音速记→自动结构化"和"本地优先 + 可追溯 AI"结合；并明确**不做留存、不做社交、不做内容分发**（见 charter 第 2、5 条）。

## 3. 目标与非目标

### 3.1 目标
- G1 捕获零摩擦：从想到到记下 ≤ 3 秒（语音长按 / 文字快捷入口）
- G2 离线可用：核心捕获、编辑、检索在无网时全可用
- G3 数据属用户：默认本地，出站最小化，可全量导出
- G4 找回一句话：自然语言检索返回带来源引用的答案
- G5 多设备一致：并发编辑不丢正文、结果与到达顺序无关（已有样板 `specs/task-sync-conflict.spec.md`）

### 3.2 非目标（charter 明确）
- 不做社交、不做内容分发
- 不以增加使用时长为优化目标
- 不引入以留存为目的的通知策略
- 不做团队协作 / 多人共享（v1 单用户）

---

## 4. 用户场景

- **S1 路上闪念**：走路/开车突然想到点子，长按图标语音说一句 → 自动转成带标题/标签的笔记，顺手识别出一个待办。
- **S2 会议/通话**：开启录音转写 → 结束后得到结构化纪要 + 提取的 action items（任务/日程）。
- **S3 离线地铁**：无网环境下记笔记、改任务、检索历史，联网后自动同步合并。
- **S4 找回**："我上个月关于订阅制的想法是啥来着" → 自然语言检索，返回笔记 + 引用，可追问。
- **S5 双设备并发**：手机和电脑同时改了同一条笔记的不同字段 → 合并不丢、与顺序无关。

---

## 5. 功能需求

模块编号：`REQ-<MODULE>-<NAME>`。优先级标在句首。完整条款由 `intent-compile` 进一步细化为 `knowledge/requirements/`。

### 5.1 捕获 Capture（`REQ-CAP-*`）

- **MUST** 系统 SHALL 提供语音速记入口：用户长按或点按后开始录音，松手即提交转写。
- **MUST** 系统 SHALL 提供文字速记入口：全局快捷入口 / 小组件 / 应用内首屏，打开即输。
- **MUST** 当设备离线时，系统 SHALL 接受所有捕获输入并本地排队，不得因无网而拒绝记录。
- **SHOULD** 系统 SHALL 提供分享/剪藏入口：从其他 app 分享文本/链接/图片到 Kite 即成一条捕获。
- **SHOULD** 系统 SHALL 支持后台持续录音转写（会议模式），并在结束时一次性结构化。
- **MAY** 系统 MAY 提供桌面端全局快捷键唤起速记。

### 5.2 转录与结构化 Transcribe & Structure（`REQ-STRUCT-*`）

- **MUST** 系统 SHALL 将语音转写为文字，并清洗（去口水词/重复），但 MUST 保留原文可追溯副本（见 5.7）。
- **MUST** 系统 SHALL 把一条捕获自动归类到笔记 / 任务 / 日程之一，并可同时产出多项（如一条语音 → 1 笔记 + 2 任务）。
- **MUST** 系统 SHALL 为每条结构化结果生成标题、摘要与标签。
- **MUST** 系统 SHALL 从转写中提取 action items（待办、日期时间），并转换为任务/日程。
- **MUST** 当 AI 对结构化结果不确定时，系统 MUST 标注置信度，不得以确定语气陈述不确定内容（charter 第 4 条）。
- **SHOULD** 系统 SHALL 允许用户选择重写风格（简洁 / 正式 / 原话保留）。
- **MAY** 系统 MAY 提供多语言转写与互译。

### 5.3 组织 Organize（`REQ-ORG-*`）

- **MUST** 系统 SHALL 维持笔记、任务、日程三类一等公民实体，并支持跨类关联（任务指向某条笔记）。
- **MUST** 系统 SHALL 支持标签与时间线视图。
- **MUST** 系统 SHALL 支持对任一实体的手动编辑，且编辑离线可用。
- **SHOULD** 系统 SHALL 支持双向链接（`[[wiki]]`）与反链。
- **SHOULD** 系统 SHALL 提供按标签/类型/时间的过滤与分组。
- **MAY** 系统 MAY 提供知识图谱可视化。

### 5.4 检索与追问 Retrieve & Ask（`REQ-RET-*`）

- **MUST** 系统 SHALL 提供自然语言检索：用户用一句话提问，返回语义相关的笔记/任务/日程。
- **MUST** 系统 SHALL 在每个检索/追问结果中附**来源引用**（指向具体笔记/段落），无依据时 MUST 回答"未找到"而非编造（charter 第 4 条）。
- **MUST** 系统 SHALL 支持追问：基于上一轮结果与引用继续提问。
- **MUST** 当离线时，系统 SHALL 提供本地关键词/标签检索；语义检索 MAY 降级提示需联网。
- **SHOULD** 系统 SHALL 支持按时间/人名/地点等实体的自然语言过滤（"上周关于小张的"）。
- **MAY** 系统 MAY 提供主动关联提醒（"这和你 X 天前的笔记相关"），但 MUST NOT 采用留存导向的推送策略（charter 第 5 条）。

### 5.5 同步 Sync（`REQ-SYNC-*`）

- **MUST** 系统 SHALL 在多设备间同步笔记/任务/日程及其元数据。
- **MUST** 系统 SHALL 支持离线写入：本地先行落盘，联网后增量同步，不得阻塞用户操作。
- **MUST** 当同一条记录在多设备并发修改产生冲突时，系统 MUST 按字段级最后写入优先合并元数据、对正文冲突保留两份并标记冲突；结果 MUST 与到达顺序无关（已落地：`services/api/sync.py` + `specs/task-sync-conflict.spec.md`）。
- **MUST** 合并逻辑 MUST 是纯函数，MUST NOT 读取系统时钟（INV-003）。
- **MUST** 系统 SHALL 保留同步变更的可审计轨迹（谁/何时/改了什么）。
- **SHOULD** 系统 SHALL 支持端到端加密同步。
- **SHOULD** 系统 SHALL 允许用户选择同步后端（自托管 / 官方 / 仅本地）。

### 5.6 隐私与数据 Privacy & Data（`REQ-PRIV-*`）

- **MUST** 用户数据 MUST 默认存储于本地设备；任何出站（上云/送模型）MUST 经过显式脱敏与最小化（INV-004）。
- **MUST** 系统 MUST 提供全量导出（开放格式：Markdown + JSON），且 MUST NOT 依赖 Kite 才可读。
- **MUST** 系统 MUST 提供一键删除某条 / 某段时间 / 全部数据，删除 MUST 不可被同步复活。
- **MUST** 系统 MUST NOT 在用户未明确同意下扩大数据出站范围（charter 第 1 条）。
- **SHOULD** 系统 SHOULD 对敏感字段（密钥/凭证/健康/财务）在采集侧即标记，禁止进入模型上下文。

### 5.7 AI 治理 AI Governance（`REQ-AI-*`）

- **MUST** 每条 AI 生成内容 MUST 可追溯到来源（原始转写 / 引用的笔记 / 模型版本 / 提示词版本）。
- **MUST** 系统 MUST 维护 AI 物料清单（模型元数据、提示词、工具配置，CycloneDX AI-BOM 标准）。
- **MUST** AI 输出 MUST 区分"事实引用"与"生成推断"，推断 MUST 标注置信度。
- **MUST** 提示词与工具定义的变更 MUST 受发布自主 D1.5 约束（带影响面上限，见 `reversibility.yaml`）。
- **MUST** 处理不可信外部输入（剪藏、分享进入的内容）的角色 MUST 不持仓库令牌、只产结构化输出（防注入）。

---

## 6. 非功能需求

| 类别 | 要求 |
| --- | --- |
| 离线 | 核心路径（捕获/编辑/本地检索）100% 离线可用；离线优先于新功能（charter 第 3 条） |
| 性能 | 语音提交到结构化结果展示 ≤ 3s（联网）；本地检索 ≤ 500ms |
| 隐私 | 默认本地；出站最小化；端到端加密 SHOULD 可选 |
| 可靠性 | 同步失败可重试且不产生数据丢失；崩溃后本地数据完整 |
| 可观测 | 本地运行账本可查；AI 调用成本/用量可统计 |
| 可达性 | 符合平台无障碍标准；支持大字体/深色 |

---

## 7. 约束与不变量（不可违背）

来自 `charter/MISSION.md` 与 `knowledge/invariants/`：

1. 用户数据属于用户，任何默认行为不得扩大数据出站范围。
2. 不以增加使用时长为优化目标。
3. 离线可用性优先于新功能。
4. 任何 AI 生成内容必须可追溯到来源，不得以确定语气陈述不确定信息。
5. 不引入以留存为目的的通知策略。
6. （INV-003）合并逻辑是纯函数，不读系统时钟。
7. （INV-004）用户数据出站必须经过脱敏层。
8. （INV-005）下发内容只接受白名单组件（服务端驱动 UI 的渲染器是高危模块）。

---

## 8. 成功度量

| 指标 | 目标 |
| --- | --- |
| 捕获延迟（想到→记下） | ≤ 3s（P50），≤ 6s（P95） |
| 离线核心路径可用率 | 100% |
| 同步冲突无丢失 | 0 条正文丢失（由属性测试守护：交换律 + 无丢失） |
| 检索命中率 | 用户标注相关 ≥ 80% |
| AI 编造率（无依据却作答） | ≤ 2% |
| 数据出站未经同意次数 | 0 |

---

## 9. 竞品参考

| 类别 | 代表 | 借鉴 | 不采纳（违反 charter） |
| --- | --- | --- | --- |
| 语音→结构化 | AudioPen、Superwhisper、Otter、Wispr Flow | 语音转写+清洗重写、action item 提取、多风格 | 留存导向的推送、社交分享 |
| AI 笔记检索 | Mem.ai、Reflect、Notion AI、Tana、Fabric | 自然语言搜索、语义关联、追问 | 强迫协作、信息流 |
| 本地优先 | Obsidian、Logseq、Anytype | 本地文件、离线、同步可选、数据所有权 | 复杂双链学习成本（v1 不做深双链） |
| 极简速记 | Flomo、闪念胶囊、闪念-AI语音笔记 | 极简入口、标签、时间线 | — |

---

## 10. 路线图

| 阶段 | 范围 | 对应方案等级 |
| --- | --- | --- |
| **M0 单机 MVP** | 文字/语音捕获 → 本地结构化 → 笔记/任务/日程 → 本地检索 | 客户端 L1.5 / 后端 L2 |
| **M1 多设备同步** | 字段级冲突合并（已验证）、离线优先同步、可审计轨迹 | 同步模块 D1 |
| **M2 自然语言检索与追问** | 语义检索、来源引用、追问、离线降级 | 检索 D1.5 |
| **M3 隐私与导出** | 端到端加密可选、全量导出、敏感字段治理 | 全轴收紧 |
| **M4 AI 治理闭环** | AI 物料清单、可追溯、置信度标注 | 与自主开发方案 L2 并轨 |

每个阶段的出口准则参照《从 L1 到 L3》方案的等级达标指标（变异得分、人审拦截率、逃逸缺陷密度等）。

---

## 11. 开放问题

- Q1 转写/结构化走端侧模型还是服务端？离线优先倾向端侧，但质量与成本权衡待定。
- Q2 端到端加密下，服务端语义检索如何实现？候选：客户端检索 / 可检索加密 / 检索降级。
- Q3 同步后端是自建还是复用现成（如 CRDT 库 / 自托管）？
- Q4 双向链接是否进 v1（与"最低心智负担"可能冲突）？

---

> 本 PRD 是活文档。变更经人评审接受后，由 `intent-compile` 编译为 `knowledge/requirements/REQ-*.md`，再下降为任务契约实现。
